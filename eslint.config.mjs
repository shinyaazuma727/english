import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // KAKU reads localStorage on mount behind a "loaded" flag so the first
      // client render matches the server-rendered (empty) markup, then
      // updates once real data is available — the standard hydration-safe
      // pattern for browser-only state. This rule doesn't distinguish that
      // from unsafe setState-in-effect usage.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
